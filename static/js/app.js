// Shared helpers. Each page template also has its own <script> block.
// HW 11 may move more logic in here once patterns stabilise.
$(function () {
  // Nothing global for now — keeping this file as an intentional placeholder
  // so templates that reference it do not 404 and teammates have an obvious
  // place to add shared behaviour.
});
